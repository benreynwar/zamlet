diff --git a/librelane/steps/common_variables.py b/librelane/steps/common_variables.py
index 9660c09..4df0011 100644
--- a/librelane/steps/common_variables.py
+++ b/librelane/steps/common_variables.py
@@ -139,3 +139,3 @@ pdn_variables = [
     Variable(
         "PDN_RAIL_OFFSET",
-        Decimal,
+        Optional[Decimal],
@@ -149,3 +149,3 @@ pdn_variables = [
     Variable(
         "PDN_VWIDTH",
-        Decimal,
+        Optional[Decimal],
@@ -160,3 +160,3 @@ pdn_variables = [
     Variable(
         "PDN_HWIDTH",
-        Decimal,
+        Optional[Decimal],
@@ -171,3 +171,3 @@ pdn_variables = [
     Variable(
         "PDN_VSPACING",
-        Decimal,
+        Optional[Decimal],
@@ -182,3 +182,3 @@ pdn_variables = [
     Variable(
         "PDN_HSPACING",
-        Decimal,
+        Optional[Decimal],
@@ -193,3 +193,3 @@ pdn_variables = [
     Variable(
         "PDN_VPITCH",
-        Decimal,
+        Optional[Decimal],
@@ -204,3 +204,3 @@ pdn_variables = [
     Variable(
         "PDN_HPITCH",
-        Decimal,
+        Optional[Decimal],
@@ -215,3 +215,3 @@ pdn_variables = [
     Variable(
         "PDN_VOFFSET",
-        Decimal,
+        Optional[Decimal],
@@ -226,3 +226,3 @@ pdn_variables = [
     Variable(
         "PDN_HOFFSET",
-        Decimal,
+        Optional[Decimal],
@@ -237,3 +237,3 @@ pdn_variables = [
     Variable(
         "PDN_CORE_RING_VWIDTH",
-        Decimal,
+        Optional[Decimal],
@@ -248,3 +248,3 @@ pdn_variables = [
     Variable(
         "PDN_CORE_RING_HWIDTH",
-        Decimal,
+        Optional[Decimal],
@@ -259,3 +259,3 @@ pdn_variables = [
     Variable(
         "PDN_CORE_RING_VSPACING",
-        Decimal,
+        Optional[Decimal],
@@ -270,3 +270,3 @@ pdn_variables = [
     Variable(
         "PDN_CORE_RING_HSPACING",
-        Decimal,
+        Optional[Decimal],
@@ -281,3 +281,3 @@ pdn_variables = [
     Variable(
         "PDN_CORE_RING_VOFFSET",
-        Decimal,
+        Optional[Decimal],
@@ -292,3 +292,3 @@ pdn_variables = [
     Variable(
         "PDN_CORE_RING_HOFFSET",
-        Decimal,
+        Optional[Decimal],
@@ -321,3 +321,3 @@ pdn_variables = [
     Variable(
         "PDN_RAIL_LAYER",
-        str,
+        Optional[str],
@@ -332,3 +332,3 @@ pdn_variables = [
     Variable(
         "PDN_RAIL_WIDTH",
-        Decimal,
+        Optional[Decimal],
@@ -343,3 +343,3 @@ pdn_variables = [
     Variable(
         "PDN_HORIZONTAL_LAYER",
-        str,
+        Optional[str],
@@ -353,3 +353,3 @@ pdn_variables = [
     Variable(
         "PDN_VERTICAL_LAYER",
-        str,
+        Optional[str],
diff --git a/librelane/steps/openroad.py b/librelane/steps/openroad.py
index eac226b..9b16c5c 100644
--- a/librelane/steps/openroad.py
+++ b/librelane/steps/openroad.py
@@ -1475,6 +1475,18 @@ class GeneratePDN(OpenROADStep):
     def run(self, state_in: State, **kwargs) -> Tuple[ViewsUpdate, MetricsUpdate]:
         kwargs, env = self.extract_env(kwargs)
         if self.config["PDN_CFG"] is None:
+            missing = [
+                variable.name
+                for variable in pdn_variables
+                if variable.pdk
+                and not variable.optional
+                and self.config[variable.name] is None
+            ]
+            if missing:
+                raise StepError(
+                    "The default PDN generator requires these PDK variables: "
+                    + ", ".join(missing)
+                )
             env["PDN_CFG"] = os.path.join(
                 get_script_dir(), "openroad", "common", "pdn_cfg.tcl"
             )
diff --git a/librelane/scripts/openroad/common/io.tcl b/librelane/scripts/openroad/common/io.tcl
index 8e67224..e8ea11f 100644
--- a/librelane/scripts/openroad/common/io.tcl
+++ b/librelane/scripts/openroad/common/io.tcl
@@ -156,15 +156,19 @@ proc read_pdn_cfg {} {
     set unset_list {
         DESIGN_IS_CORE
         PDN_ENABLE_MACROS_GRID
-        PDN_RAILS_LAYER
-        PDN_UPPER_LAYER
-        PDN_LOWER_LAYER
     }
     set ::env(DESIGN_IS_CORE) $::env(PDN_MULTILAYER)
     set ::env(PDN_ENABLE_MACROS_GRID) $::env(PDN_CONNECT_MACROS_TO_GRID)
-    set ::env(PDN_RAILS_LAYER) $::env(PDN_RAIL_LAYER)
-    set ::env(PDN_UPPER_LAYER) $::env(PDN_HORIZONTAL_LAYER)
-    set ::env(PDN_LOWER_LAYER) $::env(PDN_VERTICAL_LAYER)
+    foreach {deprecated current} {
+        PDN_RAILS_LAYER PDN_RAIL_LAYER
+        PDN_UPPER_LAYER PDN_HORIZONTAL_LAYER
+        PDN_LOWER_LAYER PDN_VERTICAL_LAYER
+    } {
+        if { [info exists ::env($current)] } {
+            set ::env($deprecated) $::env($current)
+            lappend unset_list $deprecated
+        }
+    }
     foreach key [array names ::env] {
         if { [string match PDN_* $key] } {
             set fp_name FP_$key
